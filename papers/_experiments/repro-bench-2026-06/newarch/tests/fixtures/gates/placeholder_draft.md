<!--
  Gate D KNOWN-FAIL fixture. Two independent block triggers:
  (1) an explicit placeholder marker ("[placeholder]" + "TODO:"), and
  (2) a far-under-length body (well below the 3000-word prose floor).
  Either alone blocks Gate D; both present here so the test is robust.
-->

# Results

TODO: write the results section.

The pooled effect was [placeholder] and the confidence interval was [placeholder].
