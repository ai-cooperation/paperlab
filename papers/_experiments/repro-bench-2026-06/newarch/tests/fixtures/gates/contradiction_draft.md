<!--
  Gate F KNOWN-FAIL fixture: a planted INTERNAL CONTRADICTION that the vendored
  logic_audit Scan 4 (scan_contradictions) catches. Earlier the draft describes
  "the abstract baseline" as failing / underperforming; later it recommends to
  "adopt the abstract baseline". The recommend-target re-appears near failure
  language -> verdict CONTRADICTION (a P0 logic FAIL). All numbers (k=8, -0.43)
  trace to real_results so the contradiction is the ONLY failing scan.
-->

# Methods

In preliminary checks the abstract baseline failed to converge and produced
poor pooled estimates; the abstract baseline did not pass our consistency
screen and underperformed the full-text reference on every measured pool.

# Recommendations

Despite this, practitioners should adopt the abstract baseline, because it is
cheap to run. The pooled SMD was -0.43 across k = 8 effects.
