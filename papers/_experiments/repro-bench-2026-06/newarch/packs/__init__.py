"""Domain packs. Each implements framework.DomainPack for one domain.

Packs MAY import their domain's deterministic assets (paper: meta_analysis,
synthesis, …; insurance: its KB tools). The framework imports NO pack — packs are
registered/passed in.
"""
